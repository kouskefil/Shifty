spring:
  application:
    name: planning
  data:
    mongodb:
      uri: ${DB_URI}
      database: ${DB_NAME}
  jmx:
    enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka